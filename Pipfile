[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
apache-log-parser = "*"
toml = "*"

[requires]
python_version = "3.8"

[scripts]
main = "python main.py"
add_module = "python scripts/add_module.py"
test = "python -m unittest discover tests"
test_script = "python scripts/test.py"
shell = "python"
